services:
  quantum-app:
    build: .
    container_name: quantum_search_tcc
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    # Mant̩m o container vivo para voc̻ poder rodar comandos dentro dele
    tty: true 
    stdin_open: true
